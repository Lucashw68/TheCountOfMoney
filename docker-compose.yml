version: "3"

services:

  client_web:
    build: "./client_web"
    ports:
      - "3000:3000"
    networks:
      - default
    restart: always
    command:
      "npm run start"

  api:
    build: "./api"
    ports:
      - "8081:8081"
    expose:
      - 8081
    networks:
      - default
    environment:
      - PORT=8081
    depends_on:
      - mongodb-api
    restart: on-failure

  mongodb-api:
    image: mongo:latest
    container_name: mongodb-com
    expose:
      - 27017
      - 35496
    ports:
      - "27017:27017"
    restart: on-failure
    volumes:
      - mongo-volume-com:/data/db
    networks:
      - default
    environment:
      - PORT=27017

networks:
  default:

volumes:
  mongo-volume-com:
